# Uncomment this line to define a global platform for your project
 platform :ios, '9.0'

target 'Qapp' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Qapp
  pod 'Firebase'

  target 'QappTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'QappUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
